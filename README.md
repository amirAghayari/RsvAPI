# Ticket Express

Added authentication using JWT (access + refresh tokens).

See `docs/AUTH.md` for implementation details and manual test steps.

**API docs (Swagger UI):** http://localhost:3000/api-docs
